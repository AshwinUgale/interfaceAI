# Internal planning doc — kept local. REPORT.md (the deliverable) IS committed.
/docs/DESIGN.md

# Node
node_modules/
dist/
*.log

# Env / secrets — never commit
.env
.env.*
!.env.example

# Playwright / test output
playwright-report/
test-results/
/trace/

# Temp/working evidence dirs (underscore-prefixed); real /evidence/* is committed
evidence/_*/
# Manual handoff output (requires a human operator; not part of committed evidence)
evidence/replay-handoff-manual/
dbg.ts
